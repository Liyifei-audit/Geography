# 地球自转和公转的可视化程序 —— 开发文档

> 单文件 Three.js 3D 地理教学可视化项目 | 版本 v2.0（视频版控制台、离线贴图、分析叠加层）
> 交付物：`index.html` + `assets/earth-day.jpg` + `assets/earth-night.png` + 本文档

---

## 目录

1. [项目整体介绍](#1-项目整体介绍)
2. [资源来源（全部公开权威渠道）](#2-资源来源全部公开权威渠道)
3. [功能清单对照表](#3-功能清单对照表)
4. [常见 Bug 修复方案](#4-常见-bug-修复方案)
5. [后期二次开发拓展指南](#5-后期二次开发拓展指南)

---

## 1. 项目整体介绍

本项目是一个**可直接部署、可直接教学使用、可二次开发**的单文件交互式地理天文教学网站。它以 Three.js 为 3D 引擎，用真实的黄赤交角（23.43°）、真实偏心率的标准椭圆公转轨道、以及国际公开天文算法（NOAA 太阳位置计算体系 / 标准球面天文学公式）实时模拟：

- 地球绕**倾斜地轴**的匀速自转 → 昼夜交替
- 地球沿**标准椭圆轨道**绕太阳公转 → 春夏秋冬四季更替
- **太阳直射点**在南北回归线之间往复移动
- **晨昏线**（昼半球/夜半球边界）随季节改变倾斜角度
- 春分 / 夏至 / 秋分 / 冬至四个节气在轨道上的**真实日期自动计算与标注**

### 1.1 页面布局（1:1 复刻教学课件风格）

| 区域 | 说明 |
| --- | --- |
| 顶部导航栏 | 视角切换（地心/日心）、重置、图层设置、参数复位、全屏、关于 |
| 左侧控制面板 | 模块一·图层控制（15 个开关）、模块二·实时天文数据（15 项）、模块三·动画播放控制台（自转/公转独立控制 + 时间进度条） |
| 右侧 3D 画布 | 黑色星空粒子背景 + 全屏 WebGL 渲染，底部主标题「地球自转和公转的可视化程序」 |
| 视觉风格 | 浅黄色课件面板（#faf3df）、黑色字体、复古教学风（楷体/宋体标题）、深色画布 |

> 当前默认入口为**地心近景**：首次打开即可完整看到地球昼夜纹理与纬线图层；持续播放公转时镜头会同步跟随地球。切换到「日心视角」可浏览太阳、公转轨道与节气标记，所有内容仍可通过左侧图层开关随时显示。

### 1.2 代码架构（模块化分层）

```
index.html
├── <style>       全局样式（课件风格、响应式）
└── <script>      主程序（IIFE 封装，无全局污染）
    ├── 一、CONFIG        所有可调参数集中于此（天文常量/默认状态/纹理地址）
    ├── 二、Astro         标准天文算法：黄经/赤纬/均时差/高度角/方位角/昼长/轨道位置
    ├── 三、Utils         贴图加载（失败兜底）、Canvas 文字精灵、经纬度坐标、格式化
    ├── 四、Scene         渲染器/相机/OrbitControls/星空粒子
    ├── 五、Bodies        地球（昼夜双纹理着色器）/太阳/云层/大气辉光
    ├── 六、Layers        15 个可开关图层
    ├── 七、DataPanel     实时数据面板（150ms 节流刷新）
    ├── 八、Animation     主循环：时间推进/轨道运动/自转/晨昏线/昼弧夜弧
    ├── 九、UI            按钮/滑块/输入框/全屏/关于/重置
    └── 十、init          启动入口（加载管理 + 初始化顺序）
```

### 1.3 关键技术点

- **昼夜双纹理着色器**：自定义 ShaderMaterial 按世界法线与太阳方向点积，将白天地表纹理与夜晚城市灯光纹理做 `smoothstep` 柔和过渡，晨昏线自然不生硬；同时内建平行光漫反射与海洋镜面高光，不依赖场景灯光，避免"贴图黑屏"类问题。
- **载体层级防"地球翻转"**：`orbitGroup(公转平移) → earthSystem(地轴倾斜 23.43°) → earthMesh(绕倾斜轴自转)`。地轴倾斜只施加一次，公转只平移不旋转，因此地轴在空间中恒定指向，四季与昼夜物理严格正确。
- **真实椭圆轨道**：太阳位于椭圆焦点，偏心距 e≈0.0167（标准值），由太阳黄经反推地球黄经，结合开普勒近似（中心差方程）计算日地距离，1 月初近日点、7 月初远日点自动呈现。
- **节气真实日期**：用 NOAA 太阳视黄经公式逐日扫描 + 线性插值，自动求当年春分/夏至/秋分/冬至时刻（实测 2024 年误差 < 10 分钟），并随仿真跨年自动重算。

---

## 2. 资源来源（全部公开权威渠道）

> 引擎直接内嵌于 `index.html`；地球日景与夜景贴图优先读取 `assets/` 本地文件，缺失或网络失败时自动回退到画布纹理，杜绝 404 黑屏。

### 2.1 3D 引擎（已内嵌，完全离线）

> **重要变更（v1.1）**：Three.js r152 引擎与 OrbitControls 已**直接内联进 index.html**（约 660KB），双击打开即可运行，**不再依赖任何 CDN**，彻底解决国内网络下 jsDelivr / unpkg 被墙导致的"一直加载 / 黑屏"问题。
> 注意：three.js **r148 起官方移除了 `examples/js/` 旧式非模块脚本**，网上常见的 `.../examples/js/controls/OrbitControls.js` 链接已全部 404 —— 本工程改为内嵌官方 `examples/jsm/controls/OrbitControls.js`（已机械转换为经典脚本并挂载到 `THREE.OrbitControls`）。

| 资源 | 获取方式 |
| --- | --- |
| Three.js r152 主库 | 从 `registry.npmmirror.com/three/0.152.2/files/build/three.min.js`（阿里云镜像，国内可达）下载后内嵌 |
| OrbitControls（轨道控制器） | 从 `registry.npmmirror.com/three/0.152.2/files/examples/jsm/controls/OrbitControls.js` 下载；将 `import {…} from 'three'` 改写为 `const {…} = THREE;`、`export {…}` 改写为 `THREE.… = …;` 后内嵌 |

### 2.2 地球贴图（NASA 影像，多源自动切换）

| 贴图 | 用途 | 首选地址 | 备用镜像（自动切换） |
| --- | --- | --- | --- |
| 白天地表纹理（NASA Blue Marble） | 昼半球地表 | `threejs.org/examples/textures/planets/earth_atmos_2048.jpg` | `registry.npmmirror.com/three-globe/2.45.2/files/example/img/earth-blue-marble.jpg`（另含 unpkg / jsDelivr 同图镜像） |
| 夜景灯光纹理 | 夜半球城市灯光 | `threejs.org/examples/textures/planets/earth_lights_2048.png` | `…/three-globe/2.45.2/files/example/img/earth-night.jpg`（同上多镜像） |
| 海洋镜面贴图 | 太阳高光 | `threejs.org/examples/textures/planets/earth_specular_2048.jpg` | `…/three-globe/2.45.2/files/example/img/earth-water.png`（同上多镜像） |
| 云层贴图 | 云层 | `threejs.org/examples/textures/planets/earth_clouds_1024.png` | 画布兜底 |

> **加载策略**：每张贴图按列表依次尝试，源报错或 8 秒超时自动换下一个源；**全部失败时使用内置画布兜底纹理（带经纬线的纯色地球），绝不出黑屏**。贴图失败不影响程序运行，真实贴图到达后会自动替换。

### 2.3 天文算法参考（国际公开标准）

| 算法 | 出处 |
| --- | --- |
| 太阳视黄经 / 赤纬 / 均时差 | NOAA Solar Calculator（美国国家海洋和大气管理局公开算法） |
| 太阳高度角 sin h = sinφ·sinδ + cosφ·cosδ·cosH | 标准球面天文学公式（地理/天文教材通用） |
| 昼长公式 cosω₀ = −tanφ·tanδ | 标准球面天文学公式（含极昼/极夜判定） |
| 太阳方位角公式 | NOAA 太阳方位角公开算法 |
| 轨道真近点角（中心差方程） | 开普勒方程近似解（标准天文算法） |
| 黄赤交角 ε = 23.439291° − 0.0130042°·T | IAU 标准黄赤交角公式（T 为儒略世纪数） |

### 2.4 开源项目参考（架构借鉴，非复制代码）

| 项目 | 参考点 |
| --- | --- |
| [three-globe](https://github.com/vasturiano/three-globe)（vasturiano） | 昼夜双纹理地球着色器思路、纹理资源选用 |
| [three.js 官方示例](https://github.com/mrdoob/three.js) | 地球贴图、OrbitControls、大气辉光 shader 最佳实践 |
| [stemkoski/Three.js-Examples](https://github.com/stemkoski/Three.js-Examples) | 大气层 Fresnel 辉光着色器经典实现 |

---

## 3. 功能清单对照表

| # | 需求（按规格书） | 实现位置 | 状态 |
| --- | --- | --- | --- |
| 1 | 顶部导航栏：视角切换（地球中心/太阳中心） | `bindUI() → setViewMode()` | ✅ |
| 2 | 顶部导航栏：重置 | `btn-reset-all` 事件 | ✅ |
| 3 | 顶部导航栏：全屏 | `btn-fullscreen` 事件 + `fullscreenchange` | ✅ |
| 4 | 顶部导航栏：图层设置（显隐左面板） | `btn-layers` 事件 | ✅ |
| 5 | 顶部导航栏：关于 | `#about-mask` 弹窗 | ✅ |
| 6 | 顶部导航栏：参数复位 | `btn-param-reset` 事件 | ✅ |
| 7 | 模块一：观测点开关 | `ck-obs` | ✅ |
| 8 | 模块一：赤道开关 | `ck-equator` | ✅ |
| 9 | 模块一：北回归线开关 | `ck-tropic-cancer` | ✅ |
| 10 | 模块一：南回归线开关 | `ck-tropic-capricorn` | ✅ |
| 11 | 模块一：北极圈开关 | `ck-arctic` | ✅ |
| 12 | 模块一：南极圈开关 | `ck-antarctic` | ✅ |
| 13 | 模块一：经线网格开关（每 15°） | `ck-grid-lon` | ✅ |
| 14 | 模块一：纬线网格开关（每 15°） | `ck-grid-lat` | ✅ |
| 15 | 模块一：晨昏线开关（大圆球面切割） | `ck-terminator` + `updateTerminator()` | ✅ |
| 16 | 模块一：昼弧/夜弧开关（每 15° 纬度圈按标准公式切分） | `ck-arcs` + `updateArcs()` | ✅ |
| 17 | 模块一：太阳直射光线开关 | `ck-ray` | ✅ |
| 18 | 模块一：太阳直射点开关 | `ck-subpoint` | ✅ |
| 19 | 模块一：公转轨道开关（真实椭圆） | `ck-orbit` | ✅ |
| 20 | 模块一：黄赤交角辅助线开关（地轴+赤道面+黄道面+23.43°标注） | `ck-obliquity` + `updateObliquity()` | ✅ |
| 21 | 模块一：四季节气标记点开关（真实日期） | `ck-terms` + `computeTermDates()` | ✅ |
| 22 | 模块二：观测点经度手动修改 | `inp-lon` + `onCoordInput()` | ✅ |
| 23 | 模块二：观测点纬度手动修改 | `inp-lat` + `onCoordInput()` | ✅ |
| 24 | 模块二：实时太阳高度角 | `Astro.solarElevation()` | ✅ |
| 25 | 模块二：实时太阳方位角（附加项） | `Astro.solarAzimuth()` | ✅ |
| 26 | 模块二：当日白昼时长 | `Astro.dayLengthHours()` | ✅ |
| 27 | 模块二：当日黑夜时长 | 24 − 昼长 | ✅ |
| 28 | 模块二：仿真系统当前日期/时间（UTC + 北京时间） | `refreshDataPanel()` | ✅ |
| 29 | 模块二：当前地点白昼/黑夜状态判定（含极昼/极夜） | `refreshDataPanel()` | ✅ |
| 30 | 模块二：当前太阳直射点纬度 | `Astro.declination()` | ✅ |
| 31 | 模块二：直射点经度（地球固定，附加项，与画面一致） | `subsolarPointFixed()` | ✅ |
| 32 | 模块二：当前季节 / 距下一节气天数（附加项） | `refreshDataPanel()` | ✅ |
| 33 | 模块三：地球自转独立播放/暂停 | `btn-rot-play` | ✅ |
| 34 | 模块三：地球自转速度滑块调速 | `sld-rot` | ✅ |
| 35 | 模块三：地球公转独立播放/暂停 | `btn-rev-play` | ✅ |
| 36 | 模块三：地球公转速度滑块调速 | `sld-rev` | ✅ |
| 37 | 模块三：时间进度随公转动画自动推进 + 年度进度条 | `animate()` | ✅ |
| 38 | 地球：标准球体、地轴固定倾斜 23.43° | `earthSystem.rotation.x = EPSILON_RAD` | ✅ |
| 39 | 地球：白天纹理 + 夜景灯光纹理双纹理切换，晨昏线柔和过渡 | 昼夜 ShaderMaterial | ✅ |
| 40 | 太阳光为平行光、物理光影切割 | 着色器内建平行光 + DirectionalLight | ✅ |
| 41 | 地球自转绕倾斜地轴、产生昼夜交替 | `earthMesh.rotation.y` | ✅ |
| 42 | 公转沿标准椭圆轨道、四季自动更替 | `Astro.earthPosition()` | ✅ |
| 43 | 太阳直射点在南北回归线间往复移动 | 直射点标记跟随 `dirToSun` | ✅ |
| 44 | 四节气自动标记 + 真实天文日期 | `computeTermDates()` | ✅ |
| 45 | 星空：高密度随机静态粒子、不闪烁 | `createStars()`（sizeAttenuation:false） | ✅ |
| 46 | 鼠标左键拖拽旋转视角 | OrbitControls | ✅ |
| 47 | 鼠标滚轮缩放 | OrbitControls | ✅ |
| 48 | 地心视角（环绕地球）/ 日心视角（环绕太阳看公转）平滑切换 | `setViewMode()` + 缓动补间 | ✅ |
| 49 | 所有图层勾选实时生效 | `setLayerVisible()` | ✅ |
| 50 | 手动改经纬度数据面板毫秒级重算 | `onCoordInput()` → `refreshDataPanel()` | ✅ |
| 51 | 一键重置（视角/速度/图层/时间全部恢复默认） | `btn-reset-all` | ✅ |
| 52 | 浏览器全屏 | `requestFullscreen()` | ✅ |
| 53 | 自转公转独立控制、互不干扰、可同时可单独 | 双独立时钟变量 | ✅ |
| 54 | 自适应屏幕大小、响应式画布 | `onResize()` + 媒体查询 | ✅ |
| 55 | 单文件、零依赖、双击运行、无 404 | CDN 双源 + 贴图兜底 | ✅ |

---

## 4. 常见 Bug 修复方案

> 本项目的架构已从根上规避行业常见问题，以下逐条说明成因与方案，供二次开发时参考。

### 4.0 打开一直加载 / 黑屏（CDN 被墙或脚本 404）—— v1.1 已根治

- **现象**：双击打开后一直转圈、画布全黑、无任何提示。
- **成因**：原方案用 CDN 外链加载 Three.js，存在两个致命问题：
  1. jsDelivr / unpkg 在国内经常无法访问，`<script>` 挂起会**阻塞页面解析**，`DOMContentLoaded` 永不触发，初始化代码不执行 → 一直转圈；
  2. three.js **r148 起官方移除了 `examples/js/` 目录**，网上常见的 OrbitControls 旧链接全部 404 → `new THREE.OrbitControls` 抛错、初始化中断 → 黑屏。
- **根治方案（已实现）**：
  1. Three.js 引擎与 OrbitControls **整体内嵌进 index.html**，双击即用、零网络依赖；
  2. 贴图改为**多源 CDN 自动切换 + 8 秒超时跳过 + 画布兜底**；
  3. 加载遮罩改为**首帧渲染完成即隐藏**，不再等待任何网络；
  4. 增加**全局错误兜底**：任何未捕获异常都会弹出明确中文提示，不再静默黑屏。

### 4.1 地球翻转（北极朝下 / 季节错乱）

- **成因**：把地轴倾斜角度施加在随公转旋转的父节点上，或每帧重复累加倾斜角。
- **本方案**：三层载体 `orbitGroup（只平移）→ earthSystem（只倾斜一次）→ earthMesh（只自转）`。地轴方向在空间恒定，任何公转位置季节关系都正确。
- **二次开发注意**：不要对 `orbitGroup` 施加旋转；不要每帧给 `earthSystem.rotation.x` 赋值。

### 4.2 晨昏线错乱（画错半球 / 大圆歪斜）

- **成因**：晨昏线法向取错（取了"日心→地心"而非"地心→日心"），或未随公转更新。
- **本方案**：`updateTerminator()` 每帧以 `dirToSun = -earthPos.normalize()` 为法向，用叉积构造正交基重建大圆；并做了 `dirToSun≈±Y` 的退化保护（`|y|<0.99` 时换基向量）。

### 4.3 公转轨迹偏移（轨道不在焦点 / 与太阳错位）

- **成因**：用圆方程或焦点公式写错，或轨道中心放在原点而太阳不在原点。
- **本方案**：太阳固定在世界原点（椭圆焦点），轨道点按 `r = a(1-e²)/(1+e·cosν)` 计算，方向角 = 黄经，保证太阳严格位于焦点、轨道与地球位置自洽。

### 4.4 计算数值不准（高度角/昼长/直射点漂移）

- **成因**：使用简化的固定常数公式（如把黄赤交角写死为 23.5、忽略均时差、忽略经度修正）。
- **本方案**：全部采用 NOAA 公开算法：黄赤交角按年份线性修正、真太阳时含经度 + 均时差修正、昼长公式含极昼/极夜钳制。实测 2024 节气误差 < 10 分钟。

### 4.5 星星闪烁 / 抖动

- **成因**：粒子启用了 `sizeAttenuation` 又逐帧随机更新，或粒子带透视缩放。
- **本方案**：静态 BufferGeometry + `sizeAttenuation:false`（像素大小恒定）+ 三层亮度层级，永不闪烁。

### 4.6 贴图黑屏 / 纹理不显示

- **成因**：跨域 CORS 被拦、CDN 失效、或颜色管理导致自定义着色器输出异常。
- **本方案**：
  1. 所有贴图走公开 CORS 开放 CDN；
  2. `Utils.loadTexture()` 对每张贴图提供**纯色兜底画布**，加载失败自动替换，绝不黑屏；
  3. 关闭 `ColorManagement`、输出线性色彩，自定义着色器直接输出，避免双重色彩转换。

### 4.7 切换标签页回来动画"瞬移"

- **成因**：rAF 的 delta 累积过大。
- **本方案**：`dt = Math.min(clock.getDelta(), 0.05)` 钳制，动画平滑无跳跃。

### 4.8 全屏/缩放后画布变形

- **成因**：未监听 resize 或未同步相机 aspect。
- **本方案**：`onResize()` 同步 `camera.aspect + updateProjectionMatrix() + renderer.setSize()`；面板显隐也主动触发 `onResize()`。

### 4.9 输入框无法输入（被 JS 强制改写）

- **成因**：坐标输入框在 `input` 事件里被 `toFixed()` 回写。
- **本方案**：只在 `参数复位/重置` 时写回默认值，用户输入过程不做回写，仅用钳制值更新面板。

### 4.10 视角切换瞬间跳变 / 切换后镜头丢失

- **成因**：直接改 `controls.target` 与相机位置而不处理 OrbitControls 内部球坐标。
- **本方案**：先设 `controls.target` 与 `camera.position` 再调用 `controls.update()`（update 会从实际位置重同步球坐标）；切换使用 0.7s 缓动补间，期间不强制跟随地球。

---

## 5. 后期二次开发拓展指南

### 5.1 改参数（全部集中在 `CONFIG`）

| 想改什么 | 改哪里 |
| --- | --- |
| 自转/公转默认速度 | `CONFIG.DEFAULT_ROT_SPEED` / `DEFAULT_REV_SPEED` |
| 默认仿真时间 | `CONFIG.DEFAULT_DATE`（ISO 8601 字符串） |
| 默认观测点（北京） | `CONFIG.DEFAULT_LAT` / `DEFAULT_LON` |
| 默认视角 | `CONFIG.DEFAULT_VIEW`（`'sun'` / `'earth'`） |
| 轨道大小 / 地球半径 / 太阳半径 | `ORBIT_A` / `EARTH_R` / `SUN_R` |
| 轨道偏心率 | `ORBIT_E`（保持 0.0167 为真实值；教学演示可调大至 0.05~0.1 使椭圆更明显） |
| 晨昏线过渡宽度 | 着色器 `uniforms.softness`（当前 0.12） |
| 面板刷新频率 | `animate()` 中 `lastPanelTime > 150`（毫秒） |

### 5.2 改样式（课件风格）

- 颜色变量集中在 `<style>` 的 `:root`：`--panel-bg`（面板底色）、`--ink`（字色）、`--line`（边框）、`--head-bg`（模块标题底）、`--accent`（强调色）。
- 字体：标题用 `--serif`（楷体/宋体），正文用 `--sans`。
- 画布底色：`renderer.setClearColor(0x04050a, 1)`。

### 5.3 新增一个图层（以"地轴投影带"为例）

1. 在 HTML 的图层网格里加一行：
   ```html
   <label class="layer-item"><input type="checkbox" id="ck-band" checked>地轴投影带</label>
   ```
2. 在 `buildLayers()` 中创建并注册：
   ```js
   layers.band = new THREE.Group();
   layers.band.add(/* 你的 3D 对象，可挂在 earthSystem / orbitGroup 下 */);
   earthSystem.add(layers.band);
   ```
3. 加入 `CONFIG.LAYER_DEFAULTS`（默认开关）以及 `bindLayerCheckboxes()` / `applyLayerDefaults()` 的 `ids` 数组。
4. 需要逐帧更新的逻辑写在 `animate()` 中（参考 `updateTerminator()`），用 `layers.band.visible` 判断。

### 5.4 更换贴图 / 增加贴图

- 替换 `CONFIG.TEX_*` 为任意 CORS 开放的地球贴图即可（推荐使用 three-globe 备选纹理）。
- 新增贴图（如增加一张"地形高程图"做 bump 效果）：在 `buildEarth()` 的着色器 uniforms 中加 `bumpMap`，在 fragment shader 中用 `texture2D` 采样并扰动法线。

### 5.5 增加功能

| 想加的功能 | 建议做法 |
| --- | --- |
| 显示轨道上"当前节气进度" | 复用 `lamSun`，在面板加一行百分比 |
| 多观测点对比 | 把 `obsMarker` 改为数组，UI 增加"添加观测点"按钮 |
| 显示太阳光线圆锥 | 在 `layers.ray` 中加一个半透明锥体（`ConeGeometry` + `MeshBasicMaterial` 半透明） |
| 键盘快捷键 | 在 `bindUI()` 中监听 `keydown`（空格 = 同时播放/暂停，F = 全屏） |
| 导出截图 | 用 `renderer.domElement.toDataURL('image/png')` |
| 真实尺度模式 | 在 `CONFIG` 加 `REAL_SCALE` 开关，按真实比例缩放 `SUN_R` 与 `ORBIT_A`（注意相机视野需同步放大） |
| 教学标注模式 | 在 `makeTextSprite` 基础上做可点击标注，点击弹出知识点卡片 |

### 5.6 部署

- **本地**：双击 `index.html` 即可。Three.js 引擎已内嵌，**无需联网**；贴图联网时自动加载 NASA 真实纹理，断网时自动使用画布兜底纹理，功能完整可用。
- **服务器**：把 `index.html` 放入任意静态目录（Nginx / GitHub Pages / 对象存储均可），无需任何后端。
- **完全离线教学**：将 `CONFIG.TEX_*` 指向本地图片（或保持默认画布兜底）即可 100% 离线运行。
- **升级引擎**：下载新版 `build/three.min.js` 与 `examples/jsm/controls/OrbitControls.js`，整体替换 `index.html` 中 `<script id="embed-three">…</script>`、`<script id="embed-orbit">…</script>` 两个块（OrbitControls 需按 §2.1 的 import/export 改写方式转换）。

---

## 6. v2 视频版交互说明

当前版本已补齐视频中的快捷教学流程：

- 左侧快捷控制台提供北极、南极、直射点和默认视角，以及公转、自转、晨昏线、太阳光线、经纬网、公转轨道、黄赤交角和地球五带开关。
- 春分、夏至、秋分、冬至按钮会按 NOAA 黄经算法跳转到当前模拟年份的真实时刻，并暂停公转，方便讲解。
- 黄赤交角滑块（0°～23.5°）同步更新地轴、回归线、极圈、直射点、实时数据和回归曲线；参数复位会恢复当前年份的真实值。
- “回归曲线”显示太阳直射点纬度全年变化；“星夜分布”显示全球昼夜地图、直射点和晨昏边界，跨越 ±180° 经度时分段绘制，不会出现错误斜连线。
- `assets/earth-day.jpg` 与 `assets/earth-night.png` 为本地优先贴图，适合离线演示；网络贴图失败时仍保留画布兜底纹理。
- 移动端默认隐藏左侧面板以扩大地球视野，可通过顶部“图层设置”重新打开控制台；顶部按钮支持横向滚动。

*文档版本：v2.1 · 与 index.html 同步发布*

## 7. 时间与地理探索更新

- 仿真时间支持 UTC 日期、UTC 时间、2000-2035 时间轴、前后一天步进和“当前时间”。“真实时间”锁定开启时，地球自转角度由仿真 UTC 时间推导；关闭后可独立演示自转。
- “真实时间”锁定时，自转按钮明确显示“跟随时间”并禁用，避免画面自转与天文数据分离；解除锁定后才允许独立暂停自转。
- 太阳直射点与太阳光线作为世界坐标对象更新：直射点位于地球向阳表面，光线端点与直射点一致，直射点环的法向跟随太阳方向。
- `assets/countries-50m.json` 是本地高分辨率 TopoJSON 地名候选数据，`countries-110m.json` 作为后备。应用只解码国家名称与搜索定位信息，不绘制政治边界线。
- TopoJSON 仅用于国家名称候选和搜索定位，不再向地球表面绘制边界线，避免矢量边界与卫星地形纹理产生视觉冲突。
- 左侧“地理探索”支持北京、上海、东京、伦敦、纽约、开罗、悉尼、圣保罗等城市搜索，也会把已加载国家名称加入候选项。搜索定位会更新观测点和天文数据。
- 在地球表面单击可直接拾取纬度、经度，适合课堂上查看任意地点的太阳高度、方位角和昼长。
- `assets/earth-specular.jpg`、`assets/earth-clouds.png` 已设为本地首选纹理；网络不可用时仍使用内置画布兜底。政治边界线已按教学需求移除，图层控制仅保留自然地理与天文辅助层。
- 地球纹理统一做水平经度校正（`repeat.x = -1`、`offset.x = 1`），保证 Three.js 球面 UV 与国家边界、经纬网及观测点使用同一东西方向。

本地开发预览：在项目目录运行任意静态服务器后打开 `index.html`，例如 `http://127.0.0.1:8000/index.html`。直接双击文件时，浏览器可能限制 `fetch` 读取 TopoJSON；此时 3D 核心和预置城市搜索仍可用，建议使用静态服务器加载完整国家名称候选。
